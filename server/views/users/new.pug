extends ../layouts/application.pug

block header
  = t('views.users.new.signUp')

block content
  +formFor()(action=route('users') method='post')
    +input(user, 'firstName', errors, value =t('views.users.firstName'))(type="text")
    +input(user, 'lastName', errors, value =t('views.users.lastName'))(type="text")
    +input(user, 'email', errors, value =t('views.users.email'))(type="text")
    +input(user, 'password', errors, value =t('views.users.password'))(type="password")
    +submit(user)(value=t('views.users.new.submit'))
