extends ../layouts/application.pug

block header
  = t('layouts.application.createLabel')

block content
  +formFor()(action=route('labels') method='post')
    +input(label, 'name', errors)(type="text")
    +submit(label)(value=t('views.labels.create'))