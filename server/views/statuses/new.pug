extends ../layouts/application.pug

block header
  = t('layouts.application.createStatus')

block content
  +formFor()(action=route('statuses') method='post')
    +input(status, 'name', errors, value =t('views.statuses.name'))(type="text")
    +submit(status)(value=t('views.statuses.create'))